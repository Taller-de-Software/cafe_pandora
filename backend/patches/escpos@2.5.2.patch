diff --git a/adapter/usb.js b/adapter/usb.js
index a80b729f6ddf404a8ddc6a469087b6de35bad2f7..e112281318f9eff62175109def9fed274e3a1c4f 100644
--- a/adapter/usb.js
+++ b/adapter/usb.js
@@ -3,6 +3,18 @@ const os           = require('os');
 const util          = require('util');
 const EventEmitter  = require('events');
 let usb = null;
+function initUsb() {
+  if (usb) return;
+  const mod = require('usb');
+  if (mod.usb && typeof mod.usb.on === 'function') {
+    usb = Object.create(mod.usb);
+    if (mod.findByIds) usb.findByIds = mod.findByIds;
+    if (mod.findBySerialNumber) usb.findBySerialNumber = mod.findBySerialNumber;
+  } else {
+    usb = mod;
+  }
+}
+initUsb();
 
 /**
  * [USB Class Codes ]
@@ -24,9 +36,7 @@ const IFACE_CLASS = {
  */
 function USB(vid, pid){
 
-  if (!usb) {
-    usb = require('usb');
-  }
+  initUsb();
 
   EventEmitter.call(this);
   var self = this;
